(* OCaml Version of the code *)

(* Main function to run *)
let solve_sudoku = None

(* This is to check if the number is present in the current row, column and subgrid *)
let is_valid_move grid row col num = None

(* To check if number exists in column *)
let in_column grid row num = None

(* To check if number exists in row *)
let in_row grid col num = None

(* To check if number exists in 3x3 subgrid *)
let in_subgrid grid row col num = None

(* Find the first empty cell in the sudoku grid and return its coordinates (row, col) *)
let find_empty_cell grid = None

